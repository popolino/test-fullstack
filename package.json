{
  "name": "test-fullstack",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "npm install && cd client && npm install && npm run build",
    "start": "node index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
